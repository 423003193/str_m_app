name: str_m_app
description: SecureCloud Task & Resource Manager (STRM)
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  sqflite: ^2.3.3+1
  sqflite_common_ffi: ^2.3.2
  http: ^1.2.2
  connectivity_plus: ^6.0.5
  flutter_riverpod: ^2.5.1
  path: ^1.9.0
  intl: ^0.19.0
  google_fonts: ^8.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

